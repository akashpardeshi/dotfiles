alias ls='ls -G'
alias ll='ls -lG'
alias tree='tree -C' # will output color codes if used in pipe

alias vim='nvim'

source <(fzf --zsh) # for reverse-search to use fzf

export PATH=$PATH:/Users/akashpardeshi/Library/Python/3.8/bin
